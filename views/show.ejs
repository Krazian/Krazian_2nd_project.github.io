<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title><%=threads.title%></title>
</head>
<body>
	<h1><%=threads.title%> by "<%=threads.username%>"</h1>
	<h3>Created:<%= threads.created_at %><br/>Last Updated: <%= threads.updated_at %></h3>
		<h4><%= threads.content %></h4>
		<h5>Comments</h5>
			<%users.forEach(function(users){%>
			<ul>
				<li><%=users.username%> (<%=users.created_at%>) said:
					<p><%=users.content%></p>
				</li>
			</ul>
			<%})%>



<div class="buttons">
		<button>Edit</button>
		<form action="/threads/<%=threads.id%>/?_method=DELETE" method="POST">
			<button>Delete</button>
		</form>
		<button>Like</button>
</div>
		<form action="/threads/<%=threads.id%>/?_method=PUT" method="POST">
			<input type="text" name="content" style="height:200px; width:500px;"><br/>
			<input type="submit">
		</form>
</body>
</html>